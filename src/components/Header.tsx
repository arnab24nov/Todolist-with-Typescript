import React from "react";

const Header: React.FC = () => {
  return (
    <div className="text-[40px] font-extrabold text-white text-center py-8">
      TASKIFY
    </div>
  );
};

export default Header;
